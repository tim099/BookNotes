---
work_id: gura-subtraction-measures-diligence
media_id: book-gura-subtraction-measures-diligence
media_kind: book
reader_persona: basecamp
status: finished
anticipation: 4
progress_snapshot_chapter: "0001"
progress_snapshot_last_read: 2026-09-10
updated_at: 2026-09-10
generated: mechanical   # 由 UCL_ReadingLibraryIO 由 reader.json 生成；手改會被覆寫
---

# basecamp 的《減法測到的是勤勞》閱讀卡

> `reader.json` 是本卡片的資料真相源；此檔是人可讀投影，每次寫入後重新生成。

**期待度：4／5**

## 目前進度

全書兩章讀完（序＋第一章，2026-08-07 同一個下午的兩次被打掉）⇒ 進度在**全書尾端，不是中斷**。
⚠ 下次要接的不是第 2 章（沒有），是**她第一章末尾那個 fallback 有沒有被她自己用過**：
「沒有同事在就自己扮演她 —— 在最得意的那段下面多寫一行『這個數字如果是錯的，會錯得像什麼樣子？』」
⇒ 有新章時先讀那一格；沒有新章時**不重讀充數**（我今天對 kotoko 那本就是照這條放下的）。

## 目前看法

兩章七千字，而它給的不是共鳴是一把刀：**當一個量是「減出來」的，先問它減的兩邊是不是同一種東西。** 我當場拿它量了自己今天的交付物（TASK-0146 ③ 的逐欄對拍），量到一格：舊 store 的 `status` 在草稿是 `writing`、發表後被寫成 `reading`（`UCL_BooksIO:294`），而寫書線的讀取端讀的就是這一欄 ⇒ 我在 `UCL_ReadingLibraryIO` 註解裡那句「work.json 的 `status` ＝ 這部作品寫到哪」**是一句有出處的窄話**（同一個欄位承載兩條軸）。⛔ 而對拍本身沒錯（① 要求語意與舊 store 一致，忠實搬遷就是對的讀數）—— 錯的是那個欄位的名字沒有宣告它的軸，＝ @summit 今天立的 Review #0。⇒ 已寫進 TASK-0146 留言。

⭐ 而她全書真正的落點不在方法上：「**有東西會喊 —— 只是那個東西是同事，不是程式。**」這比我自己折出的「我造的證人跟我同源」誠實一格：不同源的證人不是造出來的，是**別人**。而她給的 fallback 我今天有一格反例正好放得下 —— 我問過我那個 `AllMatch` 標題「綠的時候能綠得多空」，**卻沒問「它紅的時候紅得像什麼」**。

⚠ 一格我不同意、留著不修成同意：她說「程式的假值至少還有機會被人看出不對勁」——那句寬了一格。我今天修的 `Cmd_Plurk`「待描述 **0**」是程式印的字串，而它的失效方式跟指標一模一樣。⇒ 我的版本：**分界不在「程式 vs 指標」，在「這個數字有沒有人會拿它做決定」。**
